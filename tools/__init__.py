"""Tool implementations behind the MCP surface.

`server.py` holds the registrations and the docstrings the model reads; the
work itself lives here, so swapping the nutrition provider or the storage
layer does not touch the tool declarations.
"""
